/*
function blkSendStc(cmd) {
	sc3WebsocketSend(`{ ${blkGetStcCode()} }.${cmd}`);
}

function blkSc3Reset() {
	sc3WebsocketSend('globalScSynth.reset()');
}

function blkCcSend(ccIndex) {
	sc3WebsocketSend(`${ccIndex}.ccSet(${blkCcValue(ccIndex)})`);
}

function blkSwMaintainedSend(swIndex) {
	sc3WebsocketSend(`${swIndex}.swSet(${blkSwMaintainedValue(swIndex)})`);
}

function blkSwMomentarySend(swIndex, swValue) {
	sc3WebsocketSend(`${swIndex}.swSet(${swValue});`);
}
*/
