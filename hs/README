arrayFill requires Proc0

arrayCollect requires Proc1 (item)

overlapTexture requires Proc1 (trigger)

Out and LocalOut have results (and are not statements) so that LocalOut may be given to Mrg.

Unicode:

λ = Proc
Σ = Sum
√ = Sqrt
∞ = Infinity
≠ = Not equal
∧ = Max
∨ = Min
× = Mul
⌊ = Floor
⌈ = Ceiling
º = Ordinal (1º = first, 2º = second)
🔊 = Play
