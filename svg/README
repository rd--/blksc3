The file "Why Block SuperCollider.svg" was exported from the BlkSc3 workspace using `rec.workspace.getParentSvg()`.

It has been edited to display passably well but requires further work if it is to replace the "Why Block SuperCollider.png" file.
